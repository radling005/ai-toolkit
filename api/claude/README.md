# Claude Playground

## Setup

1.  Get an API key from Anthropic.
2.  Set it as an environment variable: `ANTHROPIC_API_KEY`.
3.  Install dependencies:
    ```bash
    pip install anthropic
    ```

## Scripts

- `hello_claude.py`: A simple script to test the connection.
