# Gemini Playground

## Setup

1.  Get an API key from Google AI Studio.
2.  Set it as an environment variable: `GEMINI_API_KEY`.
3.  Install dependencies:
    ```bash
    pip install google-generativeai
    ```

## Scripts

- `hello_gemini.py`: A simple script to test the connection.
